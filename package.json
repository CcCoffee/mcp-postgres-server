{
  "name": "postgres-server",
  "version": "1.0.0",
  "description": "Postgres数据库MCP服务器",
  "main": "build/postgres-server.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "POSTGRES_URL=localhost:5432/monitor_center POSTGRES_USERNAME=postgres POSTGRES_PASSWORD=123456 node build/postgres-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@types/pg": "^8.6.5",
    "typescript": "^5.0.0"
  }
}
